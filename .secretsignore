# Files excluded from `ripsecrets` scans. Uses .gitignore-style patterns.
# Pass `--strict-ignore` to ripsecrets when files are listed explicitly
# (e.g. `ripsecrets {staged_files}`) so this file is honoured.

# d2-rendered SVG diagrams embed base64 WOFF font data via @font-face
# `data:application/font-woff;base64,...` blocks; ripsecrets' high-entropy
# heuristic matches the font payloads as potential secrets. Generated
# artefact — re-renders deterministically from the d2 sources alongside.
design/*.svg
