name: Verify integration-test isolation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run integration-isolation checks
        run: scripts/verify-integration-isolation.sh
