<!--
The repo's PULL_REQUEST_TEMPLATE.md keeps a small amount of HTML-
comment scaffolding inside the block. The validator must accept it
once the contributor has filled in actual prose alongside the
comments.
-->

==COMMIT_MSG==

<!--
Author the squash-merge commit body here.
-->

Wire the foo into the bar.

The foo previously bypassed the bar because of a typo in
config.py; this fix routes every foo through the bar so the
metrics counter actually increments.

Closes #3
Signed-off-by: Aidan Nagorcka-Smith <aidanns@gmail.com>
==COMMIT_MSG==
