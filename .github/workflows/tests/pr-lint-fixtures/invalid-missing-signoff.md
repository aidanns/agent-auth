==COMMIT_MSG==
Add a thing.

The block has trailers but no `Signed-off-by:` line. The merge bot
(#291) pastes the block as the squash-merge body, so without the
trailer the squash commit lands DCO-noncompliant.

Closes #4
==COMMIT_MSG==
