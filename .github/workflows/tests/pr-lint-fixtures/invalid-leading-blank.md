==COMMIT_MSG==

The leading blank line above the body would render as an empty
line in `git log` since the squash commit's subject comes from the
PR title via the merge API's `commit_title` field.

Closes #1
Signed-off-by: Aidan Nagorcka-Smith <aidanns@gmail.com>
==COMMIT_MSG==
