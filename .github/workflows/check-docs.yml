# SPDX-FileCopyrightText: 2026 Aidan Nagorcka-Smith
#
# SPDX-License-Identifier: MIT

name: check-docs

# Placeholder docs-build child of `ci.yml`. agent-auth has no docs
# site today; the slot is reserved so adding one is a single-file
# change here.
#
# TODO(aidanns): replace with `mkdocs build --strict` (or similar)
# once agent-auth gains a docs site.
on:
  workflow_call:

permissions:
  contents: read

jobs:
  check-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder docs build
        run: echo "no docs site yet — placeholder always-passing job"
