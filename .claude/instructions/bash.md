# Bash-Specific Standards

Language-specific guidance for projects that ship bash scripts.

## Tooling

- **`shellcheck`** — linting for all `*.sh` files. Gate PRs.
- **`shfmt`** — formatting for all `*.sh` files. Gate PRs.
